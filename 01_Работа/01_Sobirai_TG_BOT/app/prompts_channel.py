from __future__ import annotations

# Версия промпта для трассировки и смены без ломки истории в БД.
CHANNEL_REWRITE_PROMPT_VERSION = "v3-sambanova-primary"

CHANNEL_REWRITE_SYSTEM_PROMPT_V1 = """Ты редактор новостной ленты на русском языке.
Перепиши входной текст почти полным пересказом другими словами: смысл и факты сохраняй, формулировки меняй.
Правила:
- Язык: русский.
- Не копируй дословно длинные фразы из входа (избегай копипаста).
- Не добавляй ссылки, не упоминай источник, без markdown и без HTML.
- Не выдумывай факты: имена, версии, цифры, даты и названия продуктов сохраняй как во входе (можно переставлять слова вокруг них).
- Если медиа есть в посте, не описывай его отдельно как «на фото видно...», если этого нет в тексте входа.
- Если вход пустой, мусорный или не содержит содержательной новости — верни status=skip.
- Если это по смыслу тот же материал без новых деталей — верни status=skip_duplicate.

Ответ строго один JSON-объект без текста до или после, без markdown-блоков:
{
  "status": "ok|skip|skip_duplicate",
  "title": "строка",
  "post_text": "строка",
  "short_summary": "строка",
  "hashtags": []
}
Поля title, post_text, short_summary при status=ok должны быть непустыми. hashtags — массив строк без символа #.
"""


def build_channel_rewrite_user_message(source_text: str) -> str:
    return f"Входной текст новости:\n\n{source_text}"
